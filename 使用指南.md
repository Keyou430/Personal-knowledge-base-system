# 🧠 个人知识库系统 — 小白使用指南

> 本指南面向零编程基础的用户，手把手教你从安装到使用。

---

## 📖 目录

1. [这个系统是干什么的？](#1-这个系统是干什么的)
2. [安装前的准备](#2-安装前的准备)
3. [第一步：安装 Python](#3-第一步安装-python)
4. [第二步：获取 API Key](#4-第二步获取-api-key)
5. [第三步：安装系统依赖](#5-第三步安装系统依赖)
6. [第四步：启动系统](#6-第四步启动系统)
7. [如何使用](#7-如何使用)
   - [创建领域](#71-创建领域)
   - [上传文档](#72-上传文档)
   - [智能问答](#73-智能问答)
   - [迁移和重建知识索引](#74-迁移和重建知识索引)
   - [备份和恢复](#75-备份和恢复)
   - [沉淀为经验](#76-沉淀为经验)
   - [查看和管理经验](#77-查看和管理经验)
   - [切换领域](#78-切换领域)
   - [删除领域](#79-删除领域)
8. [支持哪些文件格式？](#8-支持哪些文件格式)
9. [离线 RAG 评测](#9-离线-rag-评测)
10. [常见问题 FAQ](#10-常见问题-faq)

---

## 1. 这个系统是干什么的？

简单来说，它是一个**能理解你文档内容的 AI 助手**。

你可以把各种文档（PDF、Word、PPT、TXT、图片等）"喂"给它，然后用**自然语言提问**，它会：

- 🔍 从你的文档中找到相关内容
- 🤖 用 AI 生成一段通俗易懂的回答
- 📚 标注回答引用了哪份文档的哪部分内容

**举个例子：**
你上传了一本医学教材的 PDF，然后问"什么是高血压？"，它会从教材中找到相关段落，整理后告诉你答案，并注明"来自《内科学》第 XX 页"。

**核心特点：**
- 🏠 **本地运行**：文档存储在你自己的电脑上，不会上传到别人的服务器
- 📂 **分领域管理**：医学、编程、历史……每个领域的知识互不干扰
- 🇨🇳 **中文优化**：专门针对中文文档做了优化处理

---

## 2. 安装前的准备

你需要准备：

| 需要的东西 | 说明 |
|-----------|------|
| 一台 Windows 电脑 | Windows 10 或 11 |
| 网络连接 | 首次安装需要下载依赖和模型 |
| 约 2GB 磁盘空间 | Python 依赖 + AI 模型 |
| 硅基流动 API Key | 用于调用 AI 问答功能（下面会教你怎么获取） |

---

## 3. 第一步：安装 Python

### 3.1 检查是否已安装

按 `Win + R`，输入 `cmd`，回车打开命令提示符，输入：

```
python --version
```

如果显示类似 `Python 3.11.x` 的版本号，说明已安装，跳到第 4 步。

如果提示"不是内部或外部命令"，需要安装。

### 3.2 下载安装

1. 打开浏览器，访问 https://www.python.org/downloads/
2. 点击黄色的 **"Download Python 3.x.x"** 按钮
3. 下载完成后，双击运行安装程序
4. ⚠️ **非常重要**：安装界面底部勾选 ✅ **"Add Python to PATH"**
5. 点击 **"Install Now"**
6. 等待安装完成

### 3.3 验证安装

重新打开命令提示符（关掉之前的窗口），输入：

```
python --version
```

看到版本号就说明安装成功了。

---

## 4. 第二步：获取 API Key

系统需要一个 AI 服务的 API Key 才能回答问题。我们使用的是**硅基流动**平台（国内服务，速度快）。

### 4.1 注册账号

1. 打开浏览器，访问 https://cloud.siliconflow.cn/
2. 点击右上角 **"注册"**
3. 用手机号或邮箱完成注册

### 4.2 获取 API Key

1. 登录后，点击右上角头像 → **"API 密钥"**
2. 点击 **"创建 API 密钥"**
3. 给密钥起个名字（比如"知识库"），点击确认
4. 复制生成的密钥（格式类似 `sk-xxxxxxxxxx`）

### 4.3 填入系统

用记事本打开系统目录下的 `.env` 文件：

```
L:\个人知识库系统\.env
```

把 `your_api_key_here` 替换成你刚才复制的密钥：

```
LLM_API_KEY=sk-你的密钥粘贴在这里
```

保存并关闭文件。

> 💡 **费用说明**：硅基流动新用户有免费额度，日常问答消耗很少，基本够用。

---

## 5. 第三步：安装系统依赖

### 5.1 打开命令提示符

按 `Win + R`，输入 `cmd`，回车。

### 5.2 进入系统目录

输入以下命令（复制粘贴即可）：

```
cd /d L:\个人知识库系统
```

### 5.3 安装依赖

输入：

```
pip install -r requirements.txt
```

等待安装完成，看到类似 `Successfully installed` 的提示即可。

> ⏱️ 首次安装可能需要 5-10 分钟，取决于网速。如果中途报错，看下面的 [常见问题](#9-常见问题-faq)。

---

## 6. 第四步：启动系统

在同一个命令提示符窗口中，输入：

```
streamlit run app.py
```

稍等几秒，你会看到：

```
You can now view your Streamlit app in your browser.
Local URL: http://localhost:8501
```

系统会**自动打开浏览器**，如果没有自动打开，手动访问 http://localhost:8501

🎉 **恭喜，系统已经启动了！**

> 💡 每次使用都需要先执行启动命令。关闭命令提示符窗口 = 关闭系统。

---

## 7. 如何使用

启动后你会看到这样的界面：

```
┌─────────────────────────────────────────────────┐
│  侧边栏                    │  主页面              │
│                           │                     │
│  🧠 知识库管理             │  💬 智能问答 / 📥 上传  │
│                           │                     │
│  📂 领域切换               │                     │
│  [默认        ▼]          │                     │
│  📊 文档片段数: 0          │                     │
│                           │                     │
│  ➕ 创建新领域             │                     │
│  [领域名称______]         │                     │
│  [➕ 创建领域]             │                     │
│                           │                     │
│  🗑️ 删除领域              │                     │
│                           │                     │
│  ℹ️ 系统信息               │                     │
└─────────────────────────────────────────────────┘
```

### 7.1 创建领域

**领域**就是知识库的分类。比如你可以创建"医学"、"编程"、"读书笔记"等不同领域。

操作步骤：
1. 在左侧边栏找到 **"➕ 创建新领域"**
2. 在输入框中输入领域名称（比如"医学"）
3. 点击 **"➕ 创建领域"** 按钮
4. 看到 "领域 '医学' 创建成功！" 就完成了

> 💡 系统自带一个"默认"领域，你可以直接用，也可以创建新的。

### 7.2 上传文档

操作步骤：
1. 点击顶部的 **"📥 文档上传"** 标签页。
2. 选择目标领域，再点击 **"选择文件"**，可以一次选择多个文件。
3. 填写分类、责任人和来源；留空的分类和来源会使用默认值，责任人留空会在预览中提醒。
4. 点击 **"生成批量预览"**。系统会逐项显示新增、替换版本、重复跳过、不支持格式或解析失败，并展示拟用版本和更新时间。
5. 检查预览后点击 **"确认并入库"**。预览阶段不会写入数据库；确认后每个文件独立处理，失败文件不会阻止其他文件。
6. 根据结果中的“索引待重建”和“需要重试”清单处理异常项。

> ⏱️ 首次上传文档时，系统需要下载 Embedding 模型（约 90MB），请耐心等待。
>
> 📷 上传图片时，系统需要下载 OCR 模型（约 100MB），也会稍慢一些。

上传后系统会为文件记录内容哈希、分类、责任人、来源、版本和更新时间。同一个文件内容重复上传不会产生新版本；同名文件内容变化时，旧版本仍保留历史，但默认问答只使用新版本。DOCX/Markdown 会保留标题层级和表格，FAQ 尽量保持一问一答，PDF 会保留页码。

### 7.3 智能问答

操作步骤：
1. 点击顶部的 **"💬 智能问答"** 标签页
2. 在文本框中输入你的问题
3. 点击 **"🔍 提问"** 按钮
4. 等待 AI 生成回答（回答会实时显示）

你可以：
- 点击 **"📋 检索到的相关文档"** 查看 AI 参考了哪些内容
- 继续提问，系统会保留对话历史
- 点击 **“🗑️ 清空对话”** 重新开始

检索同时使用关键词和语义两种方式。查询编号、日期、金额或制度名称时会优先精确关键词；如果没有达到最低相关度，系统会直接提示“知识库暂无相关内容，请补充或更新相关文档”，不会让模型猜测。

### 7.4 迁移和重建知识索引

如果系统目录中已经有 `data/raw/领域名` 下的旧资料：

1. 点击顶部 **“📚 文档浏览”**。
2. 在 **“知识索引维护”** 中点击 **“迁移现有资料”**。
3. 等待逐文件结果；损坏文件会单独列出，不会阻断其他文件。
4. 如果索引失败或更换了本地模型，点击 **“重建知识索引”**。重建不会删除原始文件或文档档案。

应用启动不会自动扫描旧资料。只有上传、迁移、重建或首次问答时才会按需加载对应索引资源。

### 7.4.1 本地目录增量同步

如果资料长期放在一个本地目录，可以使用目录同步命令。命令默认只预览，不写入数据库或索引：

```
python -m core.directory_sync --source D:\KnowledgeSource --domain 默认
```

检查预览结果后，只有明确加入 `--apply` 才会应用新增和变更：

```
python -m core.directory_sync --source D:\KnowledgeSource --domain 默认 --apply
```

同步会报告新增、变更、未变化、恢复、缺失、不支持和失败项。源文件删除或改名不会删除知识库，只会标记“源文件缺失”；恢复原路径后会自动恢复可检索状态。Embedding 或语义索引不可用时，SQLite/关键词检索仍会保留，页面会提示点击“重建知识索引”，不会启动后台重建任务。

如需定时执行，请在 Windows Task Scheduler 中调用带有 `--apply` 的命令，并把输出日志写入项目 `data/logs/`；不要把 API Key 写进任务参数。一次同步同时只允许一个进程运行，锁冲突会返回配置错误。

可选参数：`--database` 指定 SQLite 文件，`--json-out` 保存 JSON 报告，`--log-file` 追加 JSONL 运行日志，`--lock-file` 指定运行锁。退出码 `0` 表示完成，`1` 表示解析、快照变化或应用失败，`2` 表示参数、源目录、数据库或锁配置错误，`3` 表示未捕获异常；`missing` 和 `unsupported` 本身不产生非零退出码。

### 7.5 备份和恢复

在 **“📚 文档浏览”** 页面找到 **“本地备份与恢复”**：

1. 在“备份目录”中填写一个空目录，点击 **“创建并校验备份”**。
2. 备份完成后会生成 `manifest.json`，记录每个数据库和原始资料文件的大小与 SHA-256。
3. 恢复时填写“备份来源目录”和“恢复目标目录”，点击 **“校验并恢复”**。
4. 如果恢复目标不是空目录，必须勾选 **“确认覆盖非空恢复目标”**；校验失败时不会修改目标目录。

备份不会删除或移动当前数据。恢复后如需让索引重新可用，请回到“知识索引维护”点击 **“重建知识索引”**，并根据页面列出的“已恢复”“缺失切片”“重试清单”处理结果。

### 7.6 沉淀为经验

问答结果下方有 **“🧠 沉淀为经验”** 按钮。它适合保存以后还会重复使用的判断、流程和处理方法。

1. 点击按钮，确认发送当前问题、回答和你选定的引用片段。
2. 等待生成经验草稿。草稿只是建议，不会自动保存。
3. 检查并编辑标题、问题 / 场景、核心结论、操作步骤和标签。
4. 点击 **“检查重复并进入审核”**。
5. 如果出现相似经验，选择 **保存为新经验**、**合并更新** 或 **放弃**。

系统只对经验卡片做重复检查，不会自动改写原有经验，也不会在未确认时保存。云端服务只接收本次操作中明确选定的问答内容和引用片段。

### 7.7 查看和管理经验

点击顶部 **“🧠 经验库”**，可以：

- 按标题、场景、结论或标签搜索；
- 展开卡片查看操作步骤、引用来源和版本记录；
- 将暂时不用的经验归档，勾选“显示已归档”后可以恢复；
- 点击 **“重建经验索引”** 修复相似检索。索引重建不会删除本地经验卡片。

首次进行经验相似检查、文档上传或相关检索时，系统可能需要加载本地模型；日常打开页面不会主动加载这些模型。

### 7.8 切换领域

在左侧边栏的 **"📂 领域切换"** 下拉框中选择不同的领域。

切换后，问答和上传都会在新选的领域中进行。每个领域的文档是**独立的**，互不影响。

### 7.9 删除领域

1. 在左侧边栏找到 **"🗑️ 删除领域"**
2. 在下拉框中选择要删除的领域（不能删除"默认"领域）
3. 点击 **"🗑️ 删除"** 按钮

> ⚠️ 删除领域会**永久删除**该领域下的所有文档数据，请谨慎操作！

---

## 8. 支持哪些文件格式？

| 格式 | 后缀名 | 说明 |
|------|--------|------|
| PDF | `.pdf` | 最常用的文档格式 |
| Word 文档 | `.docx` | 新版 Word 格式 |
| Word 旧版 | `.doc` | 需要电脑安装 Microsoft Word |
| PPT 演示 | `.pptx` | 新版 PPT 格式 |
| PPT 旧版 | `.ppt` | 需要电脑安装 Microsoft PowerPoint |
| Markdown | `.md` | 常见的笔记格式 |
| 纯文本 | `.txt` | 最简单的文本文件 |
| 图片 | `.jpg` `.png` `.bmp` | 系统会识别图片中的文字并生成描述 |

> 💡 **建议**：优先使用 `.txt`、`.md`、`.pdf`、`.docx` 格式，兼容性最好。

---

## 9. 离线 RAG 评测

系统提供不调用云端模型的本地检索评测。公开合成案例可以直接运行：

```
python -m core.evaluation --mode fixture --cases tests/fixtures/evaluation_cases.json --corpus tests/fixtures/evaluation_corpus.json
```

真实案例、基线和报告请放在 `data/evaluation/`，不会提交到代码仓库。live 模式需要数据库、领域和已有基线；只有明确加上 `--accept-baseline` 才会写入新基线。

退出码说明：`0` 通过，`1` 指标不达标，`2` 配置或索引错误，`3` 未捕获异常。报告只显示案例编号、指标和失败原因，不显示问题原文或召回内容。

### 9.1 P1 总验收门禁

发布前运行以下命令，检查自动化测试、离线评测、编译、备份恢复演练、关键词索引重建和本地 Streamlit 健康状态：

下面的示例使用 `8502`。请先在另一个命令提示符窗口执行 `streamlit run app.py --server.port 8502`；按第 6 节默认启动在 `8501` 时，将健康检查 URL 中的端口改为 `8501`。该自动检查只确认 Streamlit 进程健康，应用页面和业务流程仍需按本节手工清单确认。

```powershell
python -m core.acceptance --project-root . `
  --cases tests/fixtures/evaluation_cases.json `
  --corpus tests/fixtures/evaluation_corpus.json `
  --streamlit-url http://localhost:8502/_stcore/health `
  --json-out data/evaluation/p1-acceptance.json
```

退出码 `0` 表示阻断检查全部通过；`1` 表示至少一项阻断检查失败；`2` 表示验收输入配置错误；`3` 表示未捕获异常。报告不会保存问题原文、答案、召回片段、完整路径或密钥。`dependency_check` 的工作站全局依赖缺失只记为 warning，不会掩盖测试、评测或恢复失败。

总验收还需要按本指南手工确认：上传页的批量预览和失败重试、文档浏览页的迁移与重建、无相关资料时拒答、回答引用中的文档版本和位置、经验卡片保存/归档，以及索引故障后的重建结果。

## 10. 常见问题 FAQ

### Q: 启动时报错 "streamlit 不是内部或外部命令"

**解决方法**：用 `python -m streamlit run app.py` 代替 `streamlit run app.py`。

### Q: 安装依赖时报错 "pip 不是内部或外部命令"

**原因**：安装 Python 时没有勾选 "Add Python to PATH"。

**解决方法**：
1. 卸载 Python
2. 重新安装，**务必勾选** "Add Python to PATH"
3. 关闭所有命令提示符窗口，重新打开

### Q: 上传文档后提问，提示 "未找到相关文档"

**原因**：上传和提问不在同一个领域。

**解决方法**：检查左侧边栏的当前领域，确认上传和提问选的是同一个领域。

### Q: 回答显示 "❌ 调用 LLM API 失败"

**原因**：API Key 配置不正确或余额不足。

**解决方法**：
1. 检查 `.env` 文件中的 `LLM_API_KEY` 是否正确填写
2. 登录硅基流动控制台检查余额
3. 重启系统（关闭命令提示符，重新执行 `streamlit run app.py`）

### Q: 首次上传文档很慢

**原因**：系统需要下载 Embedding 模型（约 90MB）。

**解决方法**：耐心等待，只需下载一次。如果下载很慢，检查 `.env` 中的 `HF_ENDPOINT` 是否设置为 `https://hf-mirror.com`（国内镜像）。

### Q: 上传 .doc 或 .ppt 文件报错

**原因**：旧版 Office 格式需要电脑安装 Microsoft Office。

**解决方法**：
- 方法一：安装 Microsoft Office
- 方法二：用 Word/PPT 打开文件，另存为 `.docx` / `.pptx` 格式再上传

### Q: 系统占用了多少空间？

- Python 依赖：约 1-2 GB
- Embedding 模型：约 90 MB（首次下载）
- OCR 模型：约 100 MB（首次上传图片时下载）
- 向量数据库：取决于文档数量，一般每 100 页 PDF 约 10-50 MB

### Q: 如何关闭系统？

直接关闭命令提示符窗口即可。下次使用需要重新执行启动命令。

### Q: 能否多个人同时使用？

当前版本为单用户本地系统，不支持多用户同时访问。

---

## 📝 快速参考卡片

```
┌──────────────────────────────────────────────┐
│           快速使用流程                         │
│                                              │
│  1️⃣  打开命令提示符                            │
│     cd /d L:\个人知识库系统                    │
│                                              │
│  2️⃣  启动系统                                  │
│     streamlit run app.py                     │
│                                              │
│  3️⃣  浏览器打开 http://localhost:8501          │
│                                              │
│  4️⃣  左侧创建领域 → 上传文档 → 提问             │
│                                              │
│  ⚠️  首次使用需要：                            │
│     • 安装 Python 并勾选 Add to PATH          │
│     • 获取硅基流动 API Key 填入 .env           │
│     • pip install -r requirements.txt        │
└──────────────────────────────────────────────┘
```
